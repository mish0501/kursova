#include <vector>

#include "config.h"
#include "show.h"

using namespace std;

void auditMenu(vector<Config> &);

void allSelling(vector<Config> &);
void allByProccessorAndRam(vector<Config> &);
void allSold(vector<Config> &);
